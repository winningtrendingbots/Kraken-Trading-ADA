name: 🔮 Predict & Trade (Enhanced)

on:
  schedule:
    - cron: '*/15 * * * *'  # Cada 15 minutos
  workflow_dispatch:
    inputs:
      use_legacy_predictor:
        description: 'Usar predictor antiguo (predict_and_filter.py)'
        type: boolean
        default: false
      force_evaluation:
        description: 'Forzar evaluación de predicciones'
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  predict-and-trade:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: 📋 Log execution
        run: |
          echo "🕐 Workflow ejecutado: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "📋 Trigger: ${{ github.event_name }}"
          echo "🌿 Branch: ${{ github.ref }}"
          echo "🔧 Predictor: ${{ github.event.inputs.use_legacy_predictor == 'true' && 'Legacy' || 'Enhanced (MQL5)' }}"
      
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      # ========================================
      # PASO 1: Verificar modelo
      # ========================================
      - name: 🔍 Check if model exists
        id: check
        run: |
          echo "🔎 Verificando modelo..."
          
          if [ -d "ADAUSD_MODELS" ] && [ "$(ls -A ADAUSD_MODELS/*.pth 2>/dev/null)" ]; then
            echo "✅ Modelo encontrado"
            echo "has_model=true" >> $GITHUB_OUTPUT
            
            # Mostrar info del modelo
            if [ -f "ADAUSD_MODELS/training_summary.json" ]; then
              echo "📊 Información del modelo:"
              python3 << 'PYEOF'
              import json
            try:
                with open('ADAUSD_MODELS/training_summary.json', 'r') as f:
                    data = json.load(f)
                print(f"  Entrenado: {data.get('timestamp', 'N/A')}")
                if 'overfitting_diagnosis' in data:
                    diag = data['overfitting_diagnosis']
                    print(f"  Gap Train-Test: {diag.get('gap_train_test', 'N/A')}")
                    print(f"  Status: {diag.get('status', 'N/A')}")
            except:
                print("  (No se pudo parsear summary)")
            PYEOF
            fi
          else
            echo "⚠️ No hay modelo entrenado"
            echo "has_model=false" >> $GITHUB_OUTPUT
          fi
      
      # ========================================
      # PASO 2: Esperar modelo (si no existe)
      # ========================================
      - name: ⏳ Wait for model
        if: steps.check.outputs.has_model == 'false'
        run: |
          echo ""
          echo "⏳ ============================================"
          echo "   NO HAY MODELO ENTRENADO"
          echo "============================================"
          echo ""
          echo "🎯 Para empezar a predecir:"
          echo "   1. Ve a 'Actions' → 'Train Model'"
          echo "   2. Presiona 'Run workflow'"
          echo "   3. Espera ~15-20 minutos"
          echo ""
          echo "📊 Una vez entrenado, este workflow:"
          echo "   • Generará predicciones cada 15 min"
          echo "   • Ejecutará trades automáticamente"
          echo "   • Monitoreará órdenes cada 5 min"
          echo ""
          echo "⚠️ Este workflow se saltará hasta que exista el modelo"
          echo ""
          exit 0
      
      # ========================================
      # PASO 3: Setup Python
      # ========================================
      - name: 🐍 Setup Python
        if: steps.check.outputs.has_model == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Install dependencies
        if: steps.check.outputs.has_model == 'true'
        run: |
          echo "📦 Instalando dependencias..."
          pip install --upgrade pip
          pip install -r requirements.txt
      
      # ========================================
      # PASO 3.5: Evaluar predicciones antiguas
      # ========================================
      - name: 📊 Evaluate old predictions
        if: steps.check.outputs.has_model == 'true' && (github.event.inputs.force_evaluation == 'true' || github.event.schedule == '0 */6 * * *')
        env:
          TELEGRAM_API: ${{ secrets.TELEGRAM_API }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          echo ""
          echo "📊 ============================================"
          echo "   EVALUANDO PREDICCIONES ANTIGUAS"
          echo "============================================"
          echo ""
          
          if [ -f "evaluate_predictions.py" ]; then
            python evaluate_predictions.py || echo "⚠️ Error en evaluación (no crítico)"
          else
            echo "ℹ️ evaluate_predictions.py no encontrado (saltando)"
          fi
      
      # ========================================
      # PASO 4: Generar predicciones
      # ========================================
      - name: 🔮 Generate predictions (Enhanced)
        if: steps.check.outputs.has_model == 'true' && github.event.inputs.use_legacy_predictor != 'true'
        env:
          KRAKEN_API_KEY: ${{ secrets.KRAKEN_API_KEY }}
          KRAKEN_API_SECRET: ${{ secrets.KRAKEN_API_SECRET }}
          TELEGRAM_API: ${{ secrets.TELEGRAM_API }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          echo ""
          echo "🔮 ============================================"
          echo "   GENERANDO PREDICCIONES (ENHANCED)"
          echo "============================================"
          echo ""
          echo "✨ Usando predict_enhanced.py (enfoque MQL5)"
          echo "   - Normalización con 120 días"
          echo "   - Clasificación multi-factor (H/L/C)"
          echo "   - Confianza dinámica"
          echo ""
          
          # Verificar que existe el script
          if [ ! -f "predict_enhanced.py" ]; then
            echo "❌ predict_enhanced.py no encontrado"
            echo "⚠️ Cayendo back a predict_and_filter.py..."
            
            if python predict_and_filter.py; then
              echo "✅ Predicción completada (legacy)"
            else
              echo "❌ Error en predict_and_filter.py"
              exit 1
            fi
          else
            # Ejecutar predicción mejorada
            if python predict_enhanced.py; then
              echo ""
              echo "✅ Predicción completada (enhanced)"
              
              # Verificar que se generó el CSV
              if [ -f "trading_signals.csv" ]; then
                echo "✅ Señales guardadas en trading_signals.csv"
                
                # Mostrar última señal
                echo ""
                echo "📊 Última señal generada:"
                tail -n 1 trading_signals.csv
              else
                echo "⚠️ No se generó trading_signals.csv"
                exit 1
              fi
            else
              echo ""
              echo "❌ Error en predict_enhanced.py"
              exit 1
            fi
          fi
      
      - name: 🔮 Generate predictions (Legacy)
        if: steps.check.outputs.has_model == 'true' && github.event.inputs.use_legacy_predictor == 'true'
        env:
          KRAKEN_API_KEY: ${{ secrets.KRAKEN_API_KEY }}
          KRAKEN_API_SECRET: ${{ secrets.KRAKEN_API_SECRET }}
          TELEGRAM_API: ${{ secrets.TELEGRAM_API }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          echo ""
          echo "🔮 ============================================"
          echo "   GENERANDO PREDICCIONES (LEGACY)"
          echo "============================================"
          echo ""
          echo "⚠️ Usando predict_and_filter.py (antiguo)"
          echo ""
          
          # Ejecutar predicción con manejo de errores
          if python predict_and_filter.py; then
            echo ""
            echo "✅ Predicción completada"
            
            # Verificar que se generó el CSV
            if [ -f "trading_signals.csv" ]; then
              echo "✅ Señales guardadas en trading_signals.csv"
              
              # Mostrar última señal
              echo ""
              echo "📊 Última señal generada:"
              tail -n 1 trading_signals.csv
            else
              echo "⚠️ No se generó trading_signals.csv"
              exit 1
            fi
          else
            echo ""
            echo "❌ Error en predict_and_filter.py"
            exit 1
          fi
      
      # ========================================
      # PASO 5: Ejecutar trades
      # ========================================
      - name: 💼 Execute trades
        if: steps.check.outputs.has_model == 'true'
        env:
          KRAKEN_API_KEY: ${{ secrets.KRAKEN_API_KEY }}
          KRAKEN_API_SECRET: ${{ secrets.KRAKEN_API_SECRET }}
          TELEGRAM_API: ${{ secrets.TELEGRAM_API }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          echo ""
          echo "💼 ============================================"
          echo "   EJECUTANDO ESTRATEGIA DE TRADING"
          echo "============================================"
          echo ""
          
          # Ejecutar trader con manejo de errores
          if python trading_orchestrator.py; then
            echo ""
            echo "✅ Trading ejecutado correctamente"
            
            # Mostrar órdenes abiertas (si existen)
            if [ -f "open_orders.json" ]; then
              echo ""
              echo "📋 Órdenes abiertas:"
              cat open_orders.json | python3 -m json.tool 2>/dev/null || cat open_orders.json
            else
              echo "ℹ️ No hay órdenes abiertas"
            fi
          else
            echo ""
            echo "⚠️ Error en trading_orchestrator.py (no crítico)"
            # No hacemos exit 1 aquí porque puede ser normal (ej: sin señales válidas)
          fi
      
      # ========================================
      # PASO 6: Guardar resultados
      # ========================================
      - name: 💾 Save signals and orders
        if: steps.check.outputs.has_model == 'true'
        run: |
          echo ""
          echo "💾 ============================================"
          echo "   GUARDANDO DATOS"
          echo "============================================"
          echo ""
          
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Añadir todos los archivos relevantes
          git add trading_signals.csv 2>/dev/null || true
          git add orders_executed.csv 2>/dev/null || true
          git add prediction_tracker.csv 2>/dev/null || true
          git add open_orders.json 2>/dev/null || true
          git add kraken_trades.csv 2>/dev/null || true
          git add risk_config.json 2>/dev/null || true
          git add *.json 2>/dev/null || true
          
          # Verificar si hay cambios
          if git diff --staged --quiet; then
            echo "ℹ️ No hay nuevos datos que guardar"
          else
            # Mostrar qué se guardará
            echo "📝 Archivos modificados:"
            git diff --staged --name-only
            
            # Commit con timestamp
            TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M UTC")
            PREDICTOR="${{ github.event.inputs.use_legacy_predictor == 'true' && 'legacy' || 'enhanced' }}"
            git commit -m "🤖 Trading cycle [$PREDICTOR]: $TIMESTAMP"
            
            # Push con retry
            MAX_RETRIES=3
            for i in $(seq 1 $MAX_RETRIES); do
              if git push; then
                echo ""
                echo "✅ Datos guardados correctamente"
                break
              else
                if [ $i -lt $MAX_RETRIES ]; then
                  echo "⚠️ Reintentando push ($i/$MAX_RETRIES)..."
                  sleep 2
                  git pull --rebase
                else
                  echo "❌ Error al guardar datos después de $MAX_RETRIES intentos"
                  exit 1
                fi
              fi
            done
          fi
      
      # ========================================
      # PASO 7: Resumen final
      # ========================================
      - name: 📊 Summary
        if: always() && steps.check.outputs.has_model == 'true'
        run: |
          echo ""
          echo "📊 ============================================"
          echo "   RESUMEN DE EJECUCIÓN"
          echo "============================================"
          echo ""
          
          # Tipo de predictor usado
          if [ "${{ github.event.inputs.use_legacy_predictor }}" = "true" ]; then
            echo "🔧 Predictor: Legacy (predict_and_filter.py)"
          else
            echo "✨ Predictor: Enhanced (predict_enhanced.py - MQL5 approach)"
          fi
          echo ""
          
          # Contar archivos generados
          echo "📁 Archivos generados:"
          [ -f "trading_signals.csv" ] && echo "  ✅ trading_signals.csv" || echo "  ❌ trading_signals.csv"
          [ -f "prediction_tracker.csv" ] && echo "  ✅ prediction_tracker.csv" || echo "  ❌ prediction_tracker.csv"
          [ -f "orders_executed.csv" ] && echo "  ✅ orders_executed.csv" || echo "  ⚠️ orders_executed.csv (sin trades)"
          [ -f "kraken_trades.csv" ] && echo "  ✅ kraken_trades.csv" || echo "  ⚠️ kraken_trades.csv (sin trades cerrados)"
          [ -f "open_orders.json" ] && echo "  ✅ open_orders.json" || echo "  ⚠️ open_orders.json (sin posiciones)"
          
          echo ""
          
          # Mostrar estadísticas si existen
          if [ -f "prediction_tracker.csv" ]; then
            echo "📈 Estadísticas de predicciones:"
            python3 << 'PYEOF'
import pandas as pd
try:
    df = pd.read_csv('prediction_tracker.csv')
    total = len(df)
    evaluated = df['actual_close'].notna().sum()
    
    print(f"  Total predicciones: {total}")
    print(f"  Evaluadas: {evaluated}")
    
    if evaluated > 0:
        avg_accuracy = df[df['actual_close'].notna()]['pred_accuracy_%'].mean()
        print(f"  Accuracy promedio: {avg_accuracy:.2f}%")
except:
    pass
PYEOF
          fi
          
          echo ""
          echo "🕐 Próxima ejecución: En 15 minutos"
          echo ""
