name: Publish ADA data to GitHub Pages repo

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout trading repo
        uses: actions/checkout@v4

      - name: Generate data
        run: |
          # aquí va tu script real
          # ejemplo:
          # python generate_data.py
          echo '{"status":"ok"}' > data.json

      - name: Push data to github.io repo
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.GH_PAGES_TOKEN }}
          external_repository: winningtrendingbots/winningtrendingbots.github.io
          publish_dir: .
          destination_dir: data
